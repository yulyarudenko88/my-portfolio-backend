// const projectsList = require("../models/portfolio");

// const { HttpError, ctrlWrapper } = require("../helpers");

// const getProjectsList = async (_, res) => {
//   const result = await projectsList();
//   res.json(result);
// };

// module.exports = {
//   getProjectsList: ctrlWrapper(getProjectsList),
// };